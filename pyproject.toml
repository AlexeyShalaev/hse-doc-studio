[project]
name = "hse-doc-studio-docs"
version = "0.1.0"
description = "Documentation build tooling for hse-doc-studio"
requires-python = ">=3.12"

[dependency-groups]
docs = [
    "zensical>=0.0.37",
]

[tool.uv]
link-mode = "copy"
