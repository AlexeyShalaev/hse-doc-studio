# Architecture

Feature-Sliced Design (FSD): `app → pages → widgets → features → entities → shared`. Dependency flows downward only. Each slice exposes a public API via `index.ts`.

> placeholder — scaffolding only
