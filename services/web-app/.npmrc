# Windows: the repo path is deep, and pnpm's default virtual-store dir names
# (~120 chars) push module paths past MAX_PATH (260). Node's ESM resolver then
# fails with ERR_MODULE_NOT_FOUND (e.g. vitest → picomatch). Shorten them.
virtual-store-dir-max-length=60
